import { KeyEvent } from './key-event';

describe('KeyEvent', () => {
  it('should create an instance', () => {
    const directive = new KeyEvent();
    expect(directive).toBeTruthy();
  });
});
